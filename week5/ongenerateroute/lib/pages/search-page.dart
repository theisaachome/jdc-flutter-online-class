import 'package:flutter/material.dart';

class SearchPage extends StatelessWidget {
  static const routeName = "/search-page";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Serach Page"),),
    );
  }
}
