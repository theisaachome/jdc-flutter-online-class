import 'package:flutter/material.dart';

class WifiPage extends StatelessWidget {
  static const routeName = "/wifi-page";
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
